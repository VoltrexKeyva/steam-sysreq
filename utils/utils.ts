/**
 * Transforms a string into an upper case string
 *
 * @param str String to be transformed
 * @returns Transformed string
 */
export function strToUpper(str: string): string {
  return str.toUpperCase();
}
